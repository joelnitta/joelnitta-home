# options specified here will apply to all posts in this folder

# Enable banner style title blocks
title-block-banner: true

# Enable CC licence appendix
license: "CC BY"

# Default for table of contents
# toc-title: in profile yml files (_metadata-en.yml, _metadata-ja.yml)
toc: true
toc-location: left

# freeze computational output
# (see https://quarto.org/docs/projects/code-execution.html#freeze)
# - true: never re-render during project render, i.e., only re-render when 
#   `quarto render` is run on that file
# - false: always re-render
# - auto: only re-render during project render if the source has changed
execute:
  freeze: true
  # knitr options
  echo: true
  message: true
  warning: true

# author: in profile yml files (_metadata-en.yml, _metadata-ja.yml)

# lang: in profile yml files (_metadata-en.yml, _metadata-ja.yml) en